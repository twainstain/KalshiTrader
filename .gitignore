.env
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
# Runtime data — SQLite DBs, JSON snapshots, replay fixtures. Never checked in.
data/

# Log files (plain .log + structured .jsonl event log).
logs/

# Claude Code memory — session context + feedback snapshots. Personal,
# not for git. Restore manually if onboarding a new machine.
memory/
.DS_Store
tmp_*.html
.claude/
docs/autoresearch_original/
contracts/out/
contracts/cache/
contracts/broadcast/
.docs
